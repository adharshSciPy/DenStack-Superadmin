
  # Define Color Palette (Community)

  This is a code bundle for Define Color Palette (Community). The original project is available at https://www.figma.com/design/3hDLOS1k0w20Ie8Joh8hwD/Define-Color-Palette--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  